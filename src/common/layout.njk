<!doctype html>
<html>
  <head>
    <title>{{ file.fm.title }}</title>
    <link rel="stylesheet" href="{{ basepath }}/vendor/bulma.css" />
    <link rel="stylesheet" href="{{ basepath }}/common/index.css" />
    {% if file.fm.css %}
    <link rel="stylesheet" href="{{ basepath }}{{ file.fm.css }}" />
    {% endif %}
  </head>
  <body>
    {% include src + '/common/header.njk' %}
    <main>
      {{ file.contents | safe }}
    </main>
    <footer>フッター</footer>
  </body>
</html>
